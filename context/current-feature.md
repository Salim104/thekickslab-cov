# Current Feature

## Feature File

## Goals

## Notes

## Status
`Not Started`

## History
- `Navbar + Cart Drawer + Wishlist Drawer + Footer` — built Zustand stores `cartStore.ts` (items, totalItems/totalAmount derived, addItem merges by id+size, removeItem, updateQuantity, clearCart, localStorage persist with totals recomputed on rehydrate) and `wishlistStore.ts` (items, totalItems, addItem/removeItem/toggleItem/isInWishlist, persist); added ephemeral `uiStore.ts` so navbar icons open the sibling drawers without prop-drilling; added `formatZAR` helper to `lib/utils.ts` (R1799.99, no space). Installed shadcn `sheet`. Built `Navbar.tsx` (sticky white bar, next/image logo, Home/Shop/Contact links, lucide search/wishlist/cart/profile icons with red count badges, mobile hamburger dropdown, mounted-guard for hydration), `CartDrawer.tsx` + `WishlistDrawer.tsx` (shadcn Sheet right slide-in `sm:max-w-md`, item rows, subtotal + Clear/Checkout, Add to Cart/Remove, empty states → /shop), `Footer.tsx` (black 4-column grid, collapses on mobile, copyright bar); mounted all four in `layout.tsx`. Clerk UserButton deferred (no Clerk keys in env) — used profile-icon placeholder. `npm run build` passes. Committed together with the project scaffold as the first real commit (no remote configured — not pushed). Status: `Complete`
- `Homepage` — discovered Phase 1's Convex layer was never built, so built it as foundation: `convex/schema.ts` (products/users/wishlists/carts tables, `by_slug` + `by_bestSeller` indexes), `convex/products.ts` (`getAll` ordered desc, `getBestSellers` filtered via index, max 3), and idempotent `convex/seed.ts` (`seedProducts` mutation — 12 products carried from old `products.json`, slugified, local `/assets/...` image paths, default sizes 6–12, 3 flagged `isBestSeller`); pushed via the already-running `convex dev` watcher and seeded with `npx convex run seed:seedProducts` against `knowing-dotterel-831`. Frontend: `ConvexClientProvider.tsx` wrapping the layout body so client sections can `useQuery`; shadcn `skeleton.tsx`; single shared `ProductCard.tsx` (next/image `fill object-contain h-60` group-hover scale, red `-{discount}%` badge, whole-card `next/link` → `/product/[slug]`, uppercase name, red-600 bold sale price + gray-400 line-through original, `formatZAR`); five sections in `src/sections/` — `HeroSlider` (3 local slides, opacity fade, 5s auto-advance, white/20 lucide arrows, dot indicators, headline + black Shop Now→/shop), `FeaturesStrip` (lucide Truck/Trophy/Headphones), `BestSelling` + `ShopAll` (Convex `useQuery`, Skeleton loading + empty-state CTAs, ShopAll slices first 8), static `Deals` (black banner, "30% OFF", deals image); `app/page.tsx` composes all five. Decisions: **next/image not CldImage** (Cloudinary unconfigured — deferred); built full schema (not just products) as canonical foundation; `getAll` is newest-first. `npm run build` passes; runtime smoke-tested (home 200, all headings present, image optimizer handles spaced/parenthesized filenames). Merged `feature/homepage` into `master` via `--no-ff` (no `dev`/`main` branch and no git remote exist yet — not pushed). Status: `Complete`
- `Product Detail Page` — added two Convex queries to `convex/products.ts`: `getBySlug(slug)` (via `by_slug` index, `.unique()` → product | null) and `getRelated(brand, category, excludeSlug)` (collect + JS filter on brand-OR-category, exclude self, `.slice(0,3)` — fine at 12 rows; all seed products are category `sneakers` so related is effectively the 3 newest others). Built server route `src/app/product/[slug]/page.tsx`: `await params` (Next 16 promise), server-side `fetchQuery` from `convex/nextjs`, `notFound()` on missing slug, breadcrumb (Home → / | All → /shop | name), then `ProductDetailClient`, then related grid reusing the shared `ProductCard` (1/2/3-col). Built `src/components/ProductDetailClient.tsx` (`"use client"`): `buildGallery` pads a single Convex image to 4 thumbnails (repeats); state for mainIndex/quantity/tab; main image (`next/image` fill object-contain) with prev/next arrows shown only when gallery > 1; 4 thumbnails (`h-20 w-20`, active `border-black`); price block (bold sale + `line-through text-gray-500` original + red `-X%` badge); quantity selector (Minus/input/Plus, clamped min 1); full-width black ADD TO CART → `useCartStore.addItem` (passes `size: ""` since no size selector — CartDrawer already hides empty size) then `useUIStore.openCart()` for feedback; Add to Wishlist border button with lucide `Heart` (fills red + label flips on `isInWishlist`); Continue Shopping → /shop; `Categories: {category}`; Description/Reviews tabs (`border-b-2 border-black` active) — Description is a 2-paragraph blurb from name + brand, Reviews shows "There are no reviews yet." Decisions: **next/image not CldImage** (Cloudinary still unconfigured); **no `loading.tsx`** (page is SSR — data awaited before render, no client flash); lucide icons (react-icons not installed). Minor scope add: cart drawer auto-opens on add (uses existing `uiStore`). `npm run build` passes (route is dynamic `ƒ`); `npm run lint` clean for new files (only pre-existing Navbar setState-in-effect error remains); runtime smoke-tested — valid slug 200 with name/ADD TO CART/Related/Categories present, invalid slug 404. Merged `feature/product-detail` into `dev` via `--no-ff` per project Git Rules (dev-first; `main` deferred until Vercel preview verified), pushed `dev` to origin. Status: `Complete`
- `Shop Page` — added `getBrands` query to `convex/products.ts` (collect + Map count per brand → `[{brand, count}]` sorted by count desc then name; cheap at 12 rows) and pushed it with `npx convex dev --once` (one-shot, no watcher conflict). Built `src/app/shop/page.tsx` (static server shell + page metadata, renders `ShopClient`) and three client components: `ShopClient.tsx` (`"use client"` — owns all filter/sort/pagination state; `useQuery(getAll)` + `useQuery(getBrands)`; client-side `useMemo` filter by name-includes + brand + `price <= appliedMaxPrice`, then sort; every filter/sort change resets page to 1 inside the handlers, not an effect, to dodge the Navbar `set-state-in-effect` lint; 12-per-page slice + "Showing X – Y of Z results"; shared `sidebar` element reused in a desktop `w-1/4` aside and a `side="left"` Shadcn Sheet opened by a `lg:hidden` Filter button; Skeleton loading grid + "No products found" empty state; numbered-circle pagination + NEXT shown only when `totalPages > 1`), `FilterSidebar.tsx` (rounded-full search, `<input type=range>` R1000–R2000 step 50 with "Price: R1000 — R{value}" + black Filter button applying `pendingMaxPrice`→`appliedMaxPrice`, brand categories list with counts + "All" reset, static "NEW BALANCE 550" tag chip; exports `PRICE_MIN`/`PRICE_MAX`), `SortDropdown.tsx` (native `<select>`, exports `SortOption` = default|price_asc|price_desc). Decisions: **no `getFiltered` query** — spec's Technical Notes listed it but its own Notes say filtering is client-side; real-time search shouldn't round-trip per keystroke, so filter/sort/paginate live in `ShopClient` from `getAll` and only `getBrands` was added (a server `getFiltered` would be dead code). **Native range input + native select** instead of installing shadcn slider/select (fewer deps; only button/sheet/skeleton were installed). **Price min R1000** per design reference, not the R0 in one requirement line. **next/image** via existing ProductCard (Cloudinary still unconfigured). Known-latent: pagination never renders with the current 12-product seed (exactly one page) — correct logic, activates at >12 products. `npm run build` passes (`/shop` prerenders static `○`); `npm run lint` clean for new files (only the pre-existing Navbar error remains); runtime smoke-tested — `/shop` 200 with "Filter by price"/"Product categories"/"Showing" present. Merged `feature/shop-page` into `dev` via `--no-ff` per project Git Rules (dev-first; `main` deferred until Vercel preview verified), pushed `dev` to origin. Status: `Complete`
- `Admin Dashboard` — first Phase 3 feature; first use of `@tanstack/react-table`. **Verified env up front and both spec claims were wrong: no Clerk keys, and `NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME` is NOT set** (no `middleware.ts`/`<ClerkProvider>` either). Per user decisions: **(1) auth gate stubbed** — built all CRUD/tables/forms + Convex mutations fully, but the `/admin` auth gate is a clearly-marked `TODO(auth)` stub (in `admin/layout.tsx` + both Convex files). NO Clerk wiring because mounting `<ClerkProvider>`/middleware globally without keys would crash the live public site. **Known security gap: the new Convex mutations are unauthenticated** until Clerk + a role check land — must gate before production/`main`. **(2) image upload gated** — full custom modal built; `CldUploadWidget` renders but the upload button is disabled until the cloud name is in env (grid/delete/form-state all work). Convex (`convex/products.ts` + new `convex/users.ts`): added `create`/`update`/`remove` (named `remove`, NOT `delete` — JS reserved word → called as `api.products.remove`), shared `productFields` validator reused by create+update, and `users.getAll` (desc) + `users.updateRole`; pushed via `npx convex dev --once` to regenerate `api` types. Frontend — installed `@tanstack/react-table` (the shadcn `table.tsx` primitive does NOT pull it in) + shadcn `table`/`switch`/`dropdown-menu`/`alert-dialog`/`badge`; `src/app/admin/` routes: `layout.tsx` (navy sidebar + content shell, auth-stub TODO), `page.tsx` (`redirect()` → /admin/products, renders as 307), `products/page.tsx`, `products/new/page.tsx`, `products/[slug]/edit/page.tsx` (server `fetchQuery` getBySlug + `notFound()`, dynamic `ƒ`), `users/page.tsx`, `orders/page.tsx` (Phase 3 placeholder). Components in `src/components/admin/`: `AdminSidebar.tsx` (`"use client"`, usePathname active state, logo `brightness-0 invert`, sign-out = plain link-home stub), `AdminHeader.tsx` (server; title + optional action button), `ProductsTable.tsx` (TanStack DataTable, 11 cols incl. next/image thumbnail + green-check/gray-dash bool cells + red discount badge, name search via columnFilters, 10/page pagination shown only when >1 page, Skeleton loading + empty-state CTA, AlertDialog delete → `remove` + sonner toast), `ProductForm.tsx` (plain controlled state — NOT RHF — for the slug/discount derivation interplay; slug auto-kebabs from name until hand-edited via `slugEdited` flag, discount auto-calcs `Math.round((1-sale/orig)*100)` in the price change handlers until hand-edited via `discountEdited` flag — derivation in handlers not effects to dodge the set-state-in-effect lint; size 6–12 chip toggles, 3 shadcn Switches, manage-images button), `ImageUploadModal.tsx` (hand-built per spec — NOT shadcn Dialog: fixed overlay + `max-w-2xl` white card, thumbnail grid w/ per-image × delete, `CldUploadWidget` gated behind `NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME` with disabled fallback button, Done closes), `UsersTable.tsx` (DataTable — name/email combined search filter, role Badge, `_creationTime` → en-ZA Joined date, Make Admin/Make Customer toggle via `updateRole`). Refactor: extracted `src/components/StoreChrome.tsx` (`"use client"`, usePathname) and swapped it into `layout.tsx` so the storefront Navbar/Footer/CartDrawer/WishlistDrawer render on public routes but are hidden on `/admin/*` (admin supplies its own shell); kept global `<Toaster>`. `next.config.ts`: added `res.cloudinary.com` to `images.remotePatterns` for future uploaded images. Decisions: **mutation named `remove`** (can't export `delete`); **plain useState over RHF** in the form; **derivation in change handlers** not effects; **`@tanstack/react-table` installed separately** from the shadcn table primitive; **dev-only merge** — NOT promoted to `main` (Git Rules + unauthenticated mutations make production unsafe until Clerk lands). `npm run build` passes (11 routes; `/admin` static-redirect, edit dynamic `ƒ`, rest static `○`); `npx eslint` on new files = **0 errors**, 3 benign warnings (2× TanStack `useReactTable` React-Compiler `incompatible-library`, 1× UsersTable `useMemo` exhaustive-deps where adding the dep would defeat memoization) — cleaner than the baseline which still has the pre-existing Navbar error; runtime smoke-tested against the running dev server — `/admin`→307, products/new/users/orders→200, valid edit slug→200, bad slug→404, server-rendered headers/labels present, and StoreChrome regression confirmed (storefront keeps Navbar+Footer, admin has neither). Merged `feature/admin-dashboard` into `dev` via `--no-ff` per project Git Rules, pushed `dev` to origin; `main` deferred (must wire Clerk auth gate first). Status: `Complete`
- `Contact Page` — first feature to use Resend + Server Actions. Added shared `src/lib/contactSchema.ts` (Zod: name/subject/message `.trim().min(1)`, `z.email()` — zod v4 top-level, not deprecated `.string().email()`) imported by BOTH client and action so validation can't drift (can't export non-async from a `"use server"` file, hence the separate lib). Built `src/app/actions/sendContactEmail.ts` (`"use server"`): re-validates server-side (Server Actions are reachable via direct POST — never trust client validation alone), reads `RESEND_API_KEY`/`RESEND_TO_EMAIL` and returns `{success:false,error:"Email is not configured."}` if absent (no throw), sends via Resend `from "The Kicks Lab <onboarding@resend.dev>"`, `replyTo` = sender email, `react: createElement(ContactEmail, …)` (kept the file `.ts` — no JSX — and let Resend render the element; no separate `@react-email/render` step), returns `{success,error}` rather than throwing. `src/emails/ContactEmail.tsx` — React Email template (`Html/Body/Container/Heading/Text/Hr/Section` from `@react-email/components`), brand-navy `#0F172A`, `whiteSpace:pre-wrap` message box, typed by `ContactFormValues`. `src/components/ContactForm.tsx` (`"use client"`): react-hook-form + `zodResolver`, Name+Email in `md:grid-cols-2` / Subject / Message (`rows={6}`), placeholders per spec, inline `FormMessage` errors, `form.formState.isSubmitting` drives a `Loader2` spinner + disabled button (`w-full bg-black hover:bg-red-600`), calls the action then `toast.success("Message sent! We'll get back to you soon.")` + `form.reset()` or `toast.error("Something went wrong. Please try again.")`. `src/app/contact/page.tsx` — static server shell + metadata: centered `max-w-3xl`, "Contact Us" `text-3xl font-bold text-center`, white form card (`bg-white p-8 rounded-lg shadow-md`), 3 info cards (Email Us / Call Us / Location) from a data array w/ lucide `Mail`/`Phone`/`MapPin`, `grid-cols-1 sm:grid-cols-3`. Added shadcn UI primitives in the existing unified-`radix-ui` + `data-slot` style: `input.tsx`/`textarea.tsx` (with `focus:ring-red-500` baked in per design), `label.tsx`, `form.tsx`; **installed `sonner`** (only new dep — modern shadcn `Toaster`/`toast()` is sonner-based and the spec mandates it) → `ui/sonner.tsx` (theme hardcoded `light`, `richColors`, `top-center`; no next-themes in project) mounted in `layout.tsx`. Added `RESEND_TO_EMAIL=info@thekickslab.com` to `.env.local` (gitignored). Decisions: **sonner over radix toast** (current shadcn standard); **action stays `.ts`** via `createElement`; **schema in shared lib** not the action file. Known caveat: **`RESEND_API_KEY` is not set** locally or on Vercel — build/UI work fully but a live submit hits the graceful "Email is not configured" error-toast path until the key is added to Vercel env (couldn't verify a real email arrives). `npm run build` passes (`/contact` prerenders static `○`); `npm run lint` clean for new files (only the pre-existing Navbar `set-state-in-effect` error remains); runtime smoke-tested — `/contact` 200 with "Contact Us"/"Send Message"/info-card text/placeholders present. Per explicit user instruction ("push everything to main"), merged `feature/contact-page` → `dev` → `main` both `--no-ff` and pushed both to origin — this also promoted the previously dev-only Product Detail + Shop pages to production `main` for the first time. Status: `Complete`
- `Clerk Auth` — the feature that finally lands Clerk (every prior feature deferred it). **Env scan up front corrected the spec twice: `@clerk/nextjs` v7.4.3 WAS already installed, but `svix` was NOT, and no Clerk keys / `middleware.ts` / `<ClerkProvider>` existed.** User chose **"build assuming keys exist"** (wire unconditionally per spec, accept that build/site break until keys are added) over gating; later supplied real Clerk + Resend + Cloudinary keys, so the flow was runtime-verified. **Two Next-16 / Clerk-v7 breaking changes drove the implementation** (per AGENTS.md "this is NOT the Next.js you know"): (1) Next 16 **renamed the `middleware.ts` convention to `proxy.ts`** (middleware deprecated) — Clerk 7 is Next-16-aware (`suggestMiddlewareLocation` checks both names via `isNext16OrHigher`), so the auth middleware lives at **`src/proxy.ts`** (`clerkMiddleware` + `createRouteMatcher(["/account(.*)","/admin(.*)"])`, explicit redirect to `/sign-in?redirect_url=…`; recognized by build as "Proxy (Middleware)"); (2) Clerk v7 **dropped `<SignedIn>`/`<SignedOut>`** from `@clerk/nextjs` (replaced by `Show`) and **moved `afterSignOutUrl` off `<UserButton>` onto `<ClerkProvider>`** — so Navbar uses the **`useUser()` hook** (`isLoaded` placeholder → `/sign-in` link → `<UserButton userProfileMode="navigation" userProfileUrl="/account">`) and `afterSignOutUrl="/"` is on the provider. Built: `<ClerkProvider appearance={clerkAppearance} afterSignOutUrl="/">` wrapping `ConvexClientProvider` in `layout.tsx` + shared `src/lib/clerkAppearance.ts` (red-600 `#DC2626`, Inter, transparent card since the page supplies the shell); custom `/sign-in` + `/sign-up` catch-all routes (`[[...sign-in]]`, centered `max-w-md` white card + logo, `fallbackRedirectUrl="/"`, Clerk's own footer cross-link styled red); `/account` with server **auth-guard `layout.tsx`** (`auth()` → `redirect("/sign-in")`) + `page.tsx` (`currentUser()` + `fetchQuery(getByClerkId)` for member-since) + hand-rolled `AccountTabs` (Profile/Wishlist/Orders, `border-b-2` active, NOT shadcn tabs — none installed) + `AccountWishlist` (Convex `getByUser`, ProductCard + overlaid Trash remove via `wishlists.remove`, Skeleton + empty CTA). **Webhook** at `src/app/api/clerk-webhook/route.ts` (`POST(req: NextRequest)`) uses **`verifyWebhook` from `@clerk/nextjs/webhooks`** (does svix verification internally → no svix install needed) with `{ signingSecret: CLERK_WEBHOOK_SECRET }`; on `user.created` derives primary email + name-fallback chain and calls `fetchMutation(users.createFromClerk)`; **secret is verified in Next, so it is NOT set in Convex env** (corrected the spec). **Convex**: `users.createFromClerk` (idempotent upsert by clerkId) + `getByClerkId`; new `convex/wishlists.ts` (`getByUser` joins products + returns `wishlistId`, `addItem` deduped, `remove`, `removeByProduct`) + `convex/carts.ts` (`getByUser` joins products, `addItem` merges by user+product+size, `updateQuantity` deletes on <1, `remove`, `clearByUser`); pushed via `npx convex dev --once`. `AuthMergeProvider.tsx` merges guest localStorage cart/wishlist into Convex once per user (guarded by a ref keyed on Convex userId) then clears localStorage — **only on success** (leaves localStorage intact + retries if Convex user not yet synced). **Scope expansion (user-approved during review):** the spec's merge-and-clear left a UX hole because the drawers only read localStorage, so built **unified `useCart`/`useWishlist` hooks** (+ shared `useConvexUserId`) that read/write **Convex when signed in, Zustand+localStorage for guests**, normalizing to a common item shape; refactored `CartDrawer`/`WishlistDrawer`/`Navbar` badges/`ProductDetailClient`/`AuthMergeProvider` onto them (also fixed a latent bug where wishlist `isInWishlist` wasn't reactive). **Also resolved the admin `TODO(auth)` stub**: `admin/layout.tsx` now does a real `auth()` + Convex role check (admins only, else `redirect("/")`); `/admin/*` became dynamic `ƒ`. Known follow-ups left noted: admin **Convex mutations are still unauthenticated at the data layer** (needs `ctx.auth` + a Clerk JWT template + `ConvexProviderWithClerk` — bigger than this feature); webhook needs an **ngrok/cloudflared tunnel** + dashboard endpoint for local signups to create the Convex user (until then signed-in users fall back to the guest path since `getByClerkId` is null). **No Clerk JWT template / `ConvexProviderWithClerk`** — Convex functions take explicit `clerkId`/`userId` args per the spec signatures (simpler, no dashboard JWT config). Decisions: **`proxy.ts` not `middleware.ts`**; **`useUser()` not `<SignedIn>`**; **`afterSignOutUrl` on provider**; **`verifyWebhook` not raw svix**; **hand-rolled tabs**; **arg-based Convex auth (no ctx.auth)**; **next.config: added `img.clerk.com`** for avatars. `npm run build` passes (15 routes incl. proxy; `/account` `/admin/*` dynamic `ƒ`, sign-in/up dynamic catch-all); `npx eslint` clean for all new/changed files (only the pre-existing Navbar `set-state-in-effect` baseline error remains); **no tests** (user chose to follow the project's established no-test convention — repo has zero test infra; the only cheap unit was the webhook derivation, not worth introducing vitest for). Runtime-verified against the dev server with real keys: `/ /shop /sign-in /product/[slug]` → 200, `/account` + `/admin` → 307 redirect to `/sign-in?redirect_url=…` (auth gate live). Merged `feature/clerk-auth` into `dev` via `--no-ff` per project Git Rules; **`main` deliberately deferred** — production Vercel env has no Clerk keys, so promoting to `main` now would crash the live site; keys must be added to Vercel (dev + main envs) and the flow tested on the Vercel dev preview first. Status: `Complete`
- `Stripe Payments + Checkout` — first payments feature (Stripe test mode). **Env scan up front (per the recurring spec-is-wrong pattern): all three Stripe deps were uninstalled** (`npm install stripe @stripe/stripe-js @stripe/react-stripe-js`) **and no Stripe keys were set** — but `RESEND_API_KEY` + `NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME` ARE now set (added during Clerk Auth), so emails were runtime-verifiable. User chose **"I'll add keys now"** → real test-mode keys supplied mid-build, so the full flow was runtime-verified. **Convex**: new `orders` table (schema: userId optional, orderNumber, items[]{productId,name,image,size,quantity,price}, shipping{9 fields}, subtotal, total, status enum pending|paid|shipped|delivered|cancelled, stripePaymentIntentId, createdAt; indexes `by_orderNumber`/`by_user`/`by_paymentIntent`) + `convex/orders.ts` — `create` (**authoritative server-side pricing**: looks up each product's price from the DB, never trusts client amounts; drops lines whose product was deleted; generates `TKL-YYYYMMDD-XXXX` from UTC date + `Math.random().toString(36).slice(2,6)`; inserts status `pending`, returns {orderId, orderNumber, total}), `markPaid` (idempotent: only the pending→paid transition flips status + stores PI id and returns `justPaid:true`, so emails fire exactly once across Stripe retries), `getByOrderNumber`/`getByUser` (desc)/`getAll` (desc)/`updateStatus`; pushed via `npx convex dev --once`. **KEY DESIGN DEVIATION from spec** ("order created in the webhook"): order is created **`pending` in the `createPaymentIntent` Server Action** (before payment), webhook only flips it to `paid`. Rationale: the webhook would otherwise need the full cart+shipping in Stripe PaymentIntent metadata (500-char/value limit → fragile with image URLs), and creating up-front removes the success-page race (order always exists by orderNumber). **Tradeoff: abandoned payments leave orphan `pending` orders** (visible/cancellable in admin). **`src/lib/stripe.ts`**: critical build fix — `new Stripe("")` **throws at module import** ("Neither apiKey nor config.authenticator provided"), which broke `next build` page-data collection; refactored to a **lazy `getStripe()` singleton** (+ `isStripeConfigured`, `toStripeAmount` Rand→cents) so importing the module is side-effect-free. **`src/lib/shippingSchema.ts`**: shared Zod (9 SA provinces `z.enum`, `z.email()` v4, address2 optional) imported by BOTH the client form and the action (validation can't drift). **`src/app/actions/createPaymentIntent.ts`** (`"use server"`): resolves userId via **server-trusted `auth()`** (not a client-passed clerkId) → `getByClerkId` → userId (guest if null); re-validates shipping; creates pending order then the Stripe PaymentIntent (currency `"zar"`, `automatic_payment_methods.enabled`, `metadata.orderNumber`); returns {clientSecret, orderNumber}; guards on `isStripeConfigured`. **Webhook `src/app/api/stripe/webhook/route.ts`**: raw `req.text()` + `getStripe().webhooks.constructEvent(body, sig, STRIPE_WEBHOOK_SECRET)`; on `payment_intent.succeeded` → `markPaid(orderNumber from metadata)` → if `justPaid`, send 2 Resend emails; returns 500 on fulfilment failure so Stripe retries (idempotency guard prevents double-send); not in the proxy's protected matcher (guest checkout). **Emails** `OrderConfirmationEmail.tsx` (customer; navy header, red accents, items/totals/shipping) + `AdminOrderEmail.tsx` (leads with customer contact); shared `OrderEmailProps`; local `rand()` formatter (can't import the client util into the render path). **Checkout UI** — `app/checkout/page.tsx` (`"use client"` state machine: step/shipping/clientSecret/orderNumber/creating; PI created on shipping submit; on paid → `clear()` cart + `router.push(/checkout/success?order=)`); `StepIndicator` (1 Cart→2 Shipping→3 Payment, check/red/gray); `CartReview` (reads `useCart`, qty ±/remove, empty state); `ShippingForm` (RHF + `zodResolver`, native province `<select>`, `OrderSummary` sidebar, parent-driven `submitting`); `OrderSummary` (reused steps 2+3, `showShipping` TBD line); `PaymentForm` (module-level `loadStripe` singleton, `<Elements>`+**`<PaymentElement>`** — not split card fields, `confirmPayment` `redirect:"if_required"`, inline error on failure/no redirect, graceful notice if publishable key absent). `checkout/success/page.tsx` (server `fetchQuery(getByOrderNumber)` → order details) + static `checkout/cancelled/page.tsx`. **Admin** `OrdersTable.tsx` (TanStack: Order#/Customer/Items/Total/Status badge/Date/Actions status-dropdown→`updateStatus`+toast, search by order#-or-customer, 10/page) replacing the Phase-3 placeholder. **Account** `AccountOrders.tsx` (`getByUser`, status badges, skeleton/empty) wired into `AccountTabs` (removed placeholder). `/checkout` auto-gets storefront chrome via existing `StoreChrome` (not `/admin`) — no change needed. **Verification**: user supplied pk/sk; **I downloaded the Stripe CLI v1.42.11** to `~/stripe-cli` and got `STRIPE_WEBHOOK_SECRET` via `stripe listen --api-key … --print-secret`, wrote it to `.env.local` (**and repaired an append-without-trailing-newline glitch** that had concatenated the whsec onto the `STRIPE_SECRET_KEY` line). Ran `stripe listen --forward-to localhost:3000/api/stripe/webhook` (background). User did the live UI test (card 4242 → landed on `/checkout/success`). **Backend verified via `npx convex run`**: order `TKL-20260611-PG2R` status `paid`, `stripePaymentIntentId` set, authoritative subtotal/total R3499.98, listener showed `payment_intent.succeeded → [200]`. **Customer email arrived** (salimshaban885); **admin email did NOT** (`RESEND_TO_EMAIL`=christiansalim007 ≠ Resend account owner → blocked by `onboarding@resend.dev` test-mode restriction; code is correct, needs a verified domain or RESEND_TO_EMAIL=account address). **Discovered the empty Convex `users` table broke `/account` orders + `/admin` access** (root cause = the Clerk→Convex webhook isn't tunneled locally — documented Clerk Auth limitation, NOT a Stripe defect): the guest order had no userId and the admin role check found no user. Resolved for the demo by **seeding the user's Convex record from the Clerk API** (fetched clerkId `user_3Eyw…` via `CLERK_SECRET_KEY`, `users.createFromClerk`, then `users.updateRole`→`admin`) and **backfilling the order's userId** via a throwaway `convex/backfill.ts` (pushed, ran, deleted, re-pushed) so it shows in the account tab. `npm run build` passes (19 routes; `/checkout` + `/checkout/cancelled` static `○`, `/checkout/success` + `/api/stripe/webhook` dynamic `ƒ`); `npx eslint` on new files = 0 errors, 1 benign TanStack `useReactTable` React-Compiler warning (matches the other DataTables). Decisions: **order created `pending` in the action, `paid` in the webhook** (not webhook-only — avoids metadata limits + success-page race); **lazy `getStripe()`** (avoid import-time throw); **`PaymentElement`** not split card fields; **authoritative pricing in the Convex mutation**; **userId via server `auth()`**; **shared `shippingSchema` lib**; **native province `<select>`** (no shadcn select). Known follow-ups: `orders.create`/`updateStatus` **unauthenticated at the data layer** (same gap as the other admin mutations); abandoned payments leave `pending` orders; admin email needs a verified Resend domain; **production needs Stripe keys in Vercel + a dashboard webhook endpoint (a DIFFERENT `whsec_`)** before any `main` promotion. Merged `feature/stripe-payments` into `dev` via `--no-ff` per Git Rules; **`main` deferred** — prod Vercel has no Stripe keys, so promoting now would break live checkout; add keys + the dashboard webhook + verify on the Vercel dev preview first. Status: `Complete`
